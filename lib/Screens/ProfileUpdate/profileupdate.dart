import 'package:flutter/material.dart';

class Profileupdate extends StatelessWidget {
  const Profileupdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ProfileUpdatey'),
      ),
    );
  }
}
